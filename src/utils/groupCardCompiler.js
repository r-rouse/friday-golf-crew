// export function findScoreCardsByCourseAndDate({players, courseId, date}) {
//     const matchingScoreCards = players
//       .filter(player => 
//         player.scoreCard.courseId === courseId && player.scoreCard.date === date)
//       .map(player => player.scoreCard);
  
//     return matchingScoreCards;
//   }
  
//   // Example usage:
//   const courseId = "course1"; // The ID of the course you're interested in
//   const date = "2024-03-02"; // The specific date you're interested in
//   const matchingScoreCards = findScoreCardsByCourseAndDate(players, courseId, date);
  
//   console.log(matchingScoreCards);

  